# Employee-CRUD-RESTful-API

This projct is a simple Employee Management RESTful API built with spring Boot.
It allows basic CRUD operations  (Create, Read, Update, Delete) on employee records and mainly tested using Postman.

The goal of this project is to practice building REST APIs, handling HTTP requests, and working with a database using Spring Boot.
